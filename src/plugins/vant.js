import Vue from 'vue'

import {
   Icon,
   Swipe,
   SwipeItem,
   List,
   Overlay,
   Dialog,
   Popup,
   Toast,
   Tab,
   Tabs,
   Sticky
} from 'vant'
Vue.use(Icon)
   .use(Swipe)
   .use(SwipeItem)
   .use(List)
   .use(Overlay)
   .use(Dialog)
   .use(Popup)
   .use(Toast)
   .use(Tab)
   .use(Tabs)
   .use(Sticky)
